{
  "name": "mit-cli",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/index.js",
  "bin": {
    "sum": "./lib/index.js"
  },
  "scripts": {
    "build": "cd packages/template &&  node scripts/build.js",
    "start": "cd packages/template && node scripts/start.js",
    "preinstall": "npx only-allow pnpm",
    "lint": "eslint packages/*/{src,types}/**",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ],
  "type": "module"
}
